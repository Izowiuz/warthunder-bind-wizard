__pycache__/
*.pyc
*.tmp
*.orig
*.bak
*.bak.*

# Rendered for review by `wt-bind-preset.py --render`, never the real thing.
machine.blk*

# Built by ./harvest.py from the installed game. wt-actions.json is Gaijin's
# localisation and not ours to redistribute; the ranking is derived from the
# profiles they ship. Run ./harvest.py after cloning, or after a game patch
# adds actions.
wt-actions.json
wt-factory-rank.json

# KNEEBOARD.md and kneeboard.html are generated by `plan.py --sheet/--html`,
# but they are kept in the repo on purpose: the sheet is the point of the
# thing, and it reads on the forge without cloning. Regenerate, do not edit.
